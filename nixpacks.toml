[phases.setup]
nixPkgs = ["php82", "composer", "php82Extensions.curl", "php82Extensions.mbstring"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.start]
cmd = "php -S 0.0.0.0:$PORT -t public"
